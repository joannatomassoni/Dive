const { Show } = require('../sequelize');

// Create show
const createShow = async (req, res) => {
    try {
   
    }
    catch (err) {
        console.log(err);
        res.send(400);
    }
}


// Get all upcoming shows

// Update show

// Delete show

module.exports = {
    createShow,
}